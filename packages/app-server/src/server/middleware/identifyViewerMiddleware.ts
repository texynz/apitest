// @graph-mind
// Remove the previous line to prevent this file from being modified by the robots

// NPM Packages
import { StdlibTypes } from '@local/server-stdlib';

export default function identifyViewerMiddleware(
    app,
    config,
    factory: StdlibTypes['Factory'],
) {}
